1. Ask user for height of blocks

2. If height is < 1 or > 8 (or not an integer at all), go back to step 1

3. Print the pyramid:
    a. Define how many rows it should print
    b. Define how many spaces it should print
    c. Define how many blocks it should print